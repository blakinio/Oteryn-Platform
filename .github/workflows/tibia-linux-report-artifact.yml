name: Tibia Linux persisted route-selection export

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/tibia-linux-report-artifact.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    name: Export persisted protected-route report
    runs-on: oteryn-staging
    timeout-minutes: 7

    steps:
      - name: Inspect persisted report
        shell: bash
        run: |
          set -euo pipefail
          docker run --rm \
            --mount 'type=volume,src=oteryn-tibia-linux-analysis,dst=/analysis,readonly' \
            alpine:3.22 sh -c '
              r=$(cat /analysis/latest-client-analysis-run-id)
              p="/analysis/client-analysis/$r/report/protected-route-selection"
              test -d "$p"
              echo "run_id=$r"
              find "$p" -maxdepth 1 -type f -printf "%f\t%s bytes\n" | sort
            '

      - name: Copy persisted report
        shell: bash
        run: |
          set -euo pipefail
          out="${RUNNER_TEMP}/tibia-protected-route-selection"
          rm -rf "$out" && mkdir -p "$out"
          r="$(docker run --rm --mount 'type=volume,src=oteryn-tibia-linux-analysis,dst=/analysis,readonly' alpine:3.22 cat /analysis/latest-client-analysis-run-id)"
          cid="$(docker create --mount 'type=volume,src=oteryn-tibia-linux-analysis,dst=/analysis,readonly' alpine:3.22 true)"
          trap 'docker rm -f "$cid" >/dev/null 2>&1 || true' EXIT
          docker cp "$cid:/analysis/client-analysis/$r/report/protected-route-selection/." "$out/"

      - name: Upload persisted report
        uses: actions/upload-artifact@v4
        with:
          name: tibia-linux-protected-route-selection
          path: ${{ runner.temp }}/tibia-protected-route-selection
          retention-days: 1
