name: Native OAuth Diagnostics

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: native-oauth-diagnostics-${{ github.ref }}
  cancel-in-progress: true

jobs:
  diagnose:
    if: github.event.pull_request.number == 119
    runs-on: ubuntu-latest
    services:
      mariadb:
        image: mariadb:11.8
        env:
          MARIADB_ROOT_PASSWORD: oteryn-ci-root-password
          MARIADB_ROOT_HOST: "%"
        ports:
          - 3306:3306
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      CANARY_PROVISIONING_INTEGRATION_HOST: 127.0.0.1
      CANARY_PROVISIONING_INTEGRATION_PORT: 3306
      CANARY_PROVISIONING_INTEGRATION_ROOT_PASSWORD: oteryn-ci-root-password
      CANARY_CHARACTER_CREATE_INTEGRATION_HOST: 127.0.0.1
      CANARY_CHARACTER_CREATE_INTEGRATION_PORT: 3306
      CANARY_CHARACTER_CREATE_INTEGRATION_ROOT_PASSWORD: oteryn-ci-root-password
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          coverage: none
          extensions: pdo_mysql, pcntl, openssl
          tools: composer:v2

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Capture PHPStan diagnostics
        shell: bash
        run: |
          set +e
          composer analyse > /tmp/native-oauth-phpstan.txt 2>&1
          echo "$?" > /tmp/native-oauth-phpstan.exit
          exit 0

      - name: Capture test diagnostics
        shell: bash
        run: |
          set +e
          composer test > /tmp/native-oauth-tests.txt 2>&1
          echo "$?" > /tmp/native-oauth-tests.exit
          exit 0

      - name: Upload diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: native-oauth-diagnostics-${{ github.run_id }}
          path: |
            /tmp/native-oauth-phpstan.txt
            /tmp/native-oauth-phpstan.exit
            /tmp/native-oauth-tests.txt
            /tmp/native-oauth-tests.exit
          retention-days: 1
          if-no-files-found: error
